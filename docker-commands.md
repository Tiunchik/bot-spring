## Набор команд для нового сервера
        apt update -y && \
        apt install docker -y && \
        apt install docker-compose -y && \
        apt install docker.io -y && \
        apt install apparmor-utils -y